// This is a simple router component that redirects to the unified dashboard
// The main dashboard logic has been refactored into separate files in the dashboards folder

import UnifiedDashboard from './dashboards/UnifiedDashboard';

export default UnifiedDashboard;
